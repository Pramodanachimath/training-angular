import { RenderhighlightDirective } from './renderhighlight.directive';

describe('RenderhighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderhighlightDirective();
    expect(directive).toBeTruthy();
  });
});
