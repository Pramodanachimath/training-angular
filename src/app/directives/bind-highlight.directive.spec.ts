import { BindHighlightDirective } from './bind-highlight.directive';

describe('BindHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BindHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
